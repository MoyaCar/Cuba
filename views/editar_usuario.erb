<div class="col-md-12">
  <p>
    Presione el campo que desea cargar.
  </p>

  <div class="col-md-4">
    <form action="/admin/usuarios/<%= usuario.id %>/editar" method='post' class="row form">
      <div class="col-sm-offset-1 form-group">
        <label for="nombre">Nombre de usuario</label>
        <input type="text" name="nombre" class="form-control input-lg enfocable" required autofocus
          data-target="input[name='nombre']"
          data-teclado="alfanumerico"
          value="<%= usuario.nombre %>"
        />
      </div>

      <div class="col-sm-offset-1 form-group">
        <label for="dni">Documento (login)</label>
        <input type="text" name="nro_documento" class="form-control input-lg enfocable" required
          data-target="input[name='nro_documento']"
          value="<%= usuario.nro_documento %>"
        />
      </div>

      <div class="col-sm-offset-1 form-group">
        <label for="codigo">Código de acceso</label>
        <input type="password" name="password" class="form-control input-lg enfocable"
          data-target="input[name='password']"
        />
      </div>

      <div class="col-sm-offset-1 form-group">
        <button type="submit" class="btn btn-lg btn-success">Guardar</button>
      </div>
    </form>
  </div>

  <div class="col-md-8">
    <label>&nbsp;</label>

    <div class="form-group teclado-contenedor"></div>
  </div>
</div>
