<table class="table table-striped table-condensed text-left">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>DNI</th>
      <th>Código de Acceso</th>
      <th>Editar/Eliminar</th>
    </tr>
  </thead>

  <tbody>
    <% usuarios.each do |usuario| %>
      <tr>
        <td><%= usuario.nombre %></td>
        <td><%= usuario.dni %></td>
        <td><%= usuario.codigo %></td>
        <td>
          <a class="btn btn-lg btn-primary" href="/usuarios/<%= usuario.id %>/editar" role="button">
            <i class="glyphicon glyphicon-edit"></i>
          </a>

          <a data-method="delete" class="btn btn-lg btn-danger" href="/usuarios/<%= usuario.id %>" role="button" rel="nofollow">
            <i class="glyphicon glyphicon-remove"></i>
          </a>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<p>
  <a class="btn btn-lg btn-success" href="/usuarios/nuevo" role="button">
    <i class="glyphicon glyphicon-plus"></i>
    Cargar nuevo admin
  </a>
</p>
