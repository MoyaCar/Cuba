<p>
  <a class="btn btn-success" href="/admin/clientes/cargar" role="button">
    <i class="glyphicon glyphicon-plus"></i>
    Cargar clientes
  </a>
</p>

<table class="table table-striped table-condensed text-left">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>DNI</th>
      <th>Código de Acceso</th>
      <th>Editar/Eliminar</th>
    </tr>
  </thead>

  <tbody>
    <% usuarios.each do |usuario| %>
      <tr>
        <td><%= usuario.nombre %></td>
        <td><%= usuario.dni %></td>
        <td><%= usuario.codigo %></td>
        <td>
          <a class="btn btn-primary" href="/admin/usuarios/<%= usuario.id %>/editar" role="button">
            <i class="glyphicon glyphicon-edit"></i>
          </a>

          <a data-method="delete" class="btn btn-danger" href="/admin/usuarios/<%= usuario.id %>/eliminar" role="button" rel="nofollow">
            <i class="glyphicon glyphicon-remove"></i>
          </a>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
